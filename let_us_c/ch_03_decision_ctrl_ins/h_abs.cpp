#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    int num, as;
    cout << "Enter a number to get absolute value:";
    cin >> num;
    as = abs(num);
    cout << "The absolute value is:" << as;
    return 0;
}